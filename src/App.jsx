function App() {
  return (
    <div>
      <h1>Custom hooks</h1>
      <h2>
        Logger custom hook for useState and useReducer (check "useLogger.jsx")
      </h2>
      <h2>Localstorage custom hook (check "useLocalStorage.jsx")</h2>
    </div>
  );
}

export default App;
